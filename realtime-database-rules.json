{
  "rules": {
    "orders": {
      ".read": "auth != null && ((auth.token.email == 'unlimitedtopup001@gmail.com' || auth.token.email == 'vishnubangaru001@gmail.com') || (query.orderByChild == 'uid' && query.equalTo == auth.uid) || (query.orderByChild == 'accountEmail' && query.equalTo == auth.token.email))",
      ".write": "auth != null",
      ".indexOn": ["uid", "accountEmail"]
    },
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
